# https://pypi.org works as a maven repository for python packages
# The usage of pip installs the packages in the current environment
# Libraries hold a set of modules, which are files with python code
